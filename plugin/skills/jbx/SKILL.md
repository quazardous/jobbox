---
name: jbx
description: What JobBox is doing here, and what to do with a job that has detached.
disable-model-invocation: true
---

# jbx

Run `jbx help $ARGUMENTS` and show what it prints.

With a job id, that is the page for that job. With nothing, it is every
verb. Answer any follow-up from `jbx why`, `jbx stats` or `jbx status
<id>` rather than from memory — the binary installed here is the only
thing that knows its own version.

Do not summarise the discipline from this file. It is written in the
program, said once per session by its `SessionStart` hook, and argued in
`jbx why`. A second copy here would be a second copy to keep in step,
and the one that drifts is always the copy.
