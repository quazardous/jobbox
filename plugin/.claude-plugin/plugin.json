{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "jbx",
  "displayName": "JobBox",
  "description": "Wrap every command line; detach the ones that turn out to be long. Declares the announcing hooks too, so an ending reports itself unasked — `jbx init` alone declares only the one that wraps.",
  "version": "0.14.0",
  "author": { "name": "quazardous", "url": "https://github.com/quazardous" },
  "homepage": "https://github.com/quazardous/jobbox",
  "repository": "https://github.com/quazardous/jobbox",
  "license": "MIT",
  "keywords": ["background", "jobs", "hooks", "time"],
  "experimental": { "monitors": "./monitors/monitors.json" }
}
